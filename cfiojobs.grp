# use a coma',' as ip list delimiter,
# '--' means: use an extra file to store ip list in conf/<grp_name>.grp
#grp            user    port      bknd_grps   sh_mode     conf_file/ip_addr_list/hostname_list
grp0            root    22        none        none        --
ceph3           root    22        ceph4       none        ceph1,ceph2,ceph3

ceph4           root    22        none        remote      ceph2 
ceph5           root    22        none        local       ceph2 
